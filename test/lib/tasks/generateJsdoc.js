const {test} = require("ava");
const sinon = require("sinon");
const mock = require("mock-require");

const generateJsdoc = require("../../../lib/tasks/generateJsdoc");

test("jsdocGenerator", async (t) => {

});
